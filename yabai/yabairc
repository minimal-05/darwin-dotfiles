#!/usr/bin/env sh

sudo yabai --load-sa
yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
yabai -m signal --add label=sbar_space_created event=space_created action="sketchybar --trigger spaces_refresh"
yabai -m signal --add label=sbar_space_destroyed event=space_destroyed action="sketchybar --trigger spaces_refresh"

yabai -m config mouse_follows_focus off \
  focus_follows_mouse autofocus \
  window_zoom_persist off \
  window_placement second_child \
  window_shadow float \
  window_opacity off \
  window_opacity_duration 0.2 \
  active_window_opacity 1.0 \
  normal_window_opacity 0.8 \
  insert_feedback_color 0xff9dd274 \
  split_ratio 0.50 \
  mouse_modifier fn \
  mouse_action1 move \
  mouse_action2 resize \
  mouse_drop_action swap \
  external_bar all:46:0 \
  top_padding 8 \
  bottom_padding 8 \
  left_padding 8 \
  right_padding 8 \
  window_gap 8

# Exclude problematic apps from being managed:
yabai -m rule --add app="^(LuLu|Calculator|Autodesk Fusion|Fusion|Software Update|Exam.net|Dictionary|VLC|System Preferences|System Settings|zoom.us|Photo Booth|Archive Utility|Python|LibreOffice|App Store|Steam|Alfred|Activity Monitor|Simulator|Tailscale|Karabiner-Elements)$" manage=off
yabai -m rule --add label="Finder" app="^Finder$" title="(Co(py|nnect)|Move|Info|Pref)" manage=off
yabai -m rule --add label="Safari" app="^Safari$" title="^(General|(Tab|Password|Website|Extension)s|AutoFill|Se(arch|curity)|Privacy|Advance)$" manage=off
yabai -m rule --add label="About This Mac" app="System Information" title="About This Mac" manage=off
yabai -m rule --add label="Select file to save to" app="^Inkscape$" title="Select file to save to" manage=off
yabai -m rule --add bundle="^com.apple.iphonesimulator$" manage=off

# Shell surfaces are overlays and the settings window is a dialog -- neither
# belongs in the tiling layout. qs-switch adds this too, under the same label so
# the two replace each other; it lives here as well because the bar now starts
# from launchd at login, where qs-switch never runs.
yabai -m rule --add label=quickshell app="^quickshell$" manage=off
yabai -m config layout bsp
yabai -m rule --apply
